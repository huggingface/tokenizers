name: Delete doc comment trigger

on:
  pull_request:
    types: [ closed ]


jobs:
  delete:
    uses: huggingface/doc-builder/.github/workflows/delete_doc_comment_trigger.yml@cf20b09f274383f0d91a7055c5e43f0a2ab3d1a3  # main
    with:
      pr_number: ${{ github.event.number }}