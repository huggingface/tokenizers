# Environment for the e2e tests. The transformers stack is heavy and
# resolves its own tokenizers wheel, so it lives in dedicated venvs
# (.venv-e2e / .venv-e2e-release) — see the e2e targets in the Makefile.
transformers[torch]>=5,<6
datasets
pytest
