[package]
name = "tokenizers-python"
# Kept equal to pyproject.toml's `[project] version`. `src/lib.rs` reports this as `__version__`,
# so a mismatch would make pip and the module disagree about it.
version = "0.23.1"
edition = "2024"
publish = false

[lib]
name = "tokenizers"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "=0.29", default-features = false, features = [
  "macros",
  "extension-module",
  "abi3-py310",
  "experimental-inspect",
] }
# Same three crates as `bindings/node`, for the same reason (see its Cargo.toml): only what the
# pipeline encode path needs, not the pre-v1 engine's umbrella.
tk-encode    = { path = "../../tokenizers/tk-encode", default-features = false, features = [
  "parallelism",
  "normalizers",
  "bpe",
  "unigram",
  "wordpiece",
  "wordlevel",
  "unicode-scripts",
] }
tk-serialize = { path = "../../tokenizers/tk-serialize", features = [
  "parallelism",
  "normalizers",
  "bpe",
  "unigram",
  "wordpiece",
  "wordlevel",
  "unicode-scripts",
] }
tk-convert   = { path = "../../tokenizers/tk-convert", default-features = false }
